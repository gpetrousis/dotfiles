# Overwrite the history bindings
# Use <Up> to go back in history
bindkey "^[OA" history-beginning-search-backward
# Use <Down> to go forward in history
bindkey "^[OB" history-beginning-search-forward